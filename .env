# -----------------------------
# POSTGRES CONFIGURATION
# -----------------------------
POSTGRES_USER=gilad
POSTGRES_PASSWORD=gilad
POSTGRES_DB=gis
POSTGRES_HOST=postgis
POSTGRES_PORT=5432

# -----------------------------
# DUCKDB JOB CONFIGURATION
# -----------------------------
# JOB_TASK options:
#   join    → demo spatial join between cities and areas
#   ingest  → land a GeoJSON and publish to marts.my_points
#   buffers → make 500m city buffers
#   checks  → basic data quality checks
JOB_TASK=join

# Where results should be written
#   postgis → back into PostGIS schema
#   parquet → parquet files in the lake
JOB_SINKS=postgis,parquet

# Logical dataset name for the current job
DATASET_NAME=city_in_area

# -----------------------------
# LAKE LAYOUT (DuckDB “data lake” on disk)
# -----------------------------
LAKE_ROOT=/work/lake          # container path (mapped to ./data/lake on host)
LAKE_TIER=marts               # raw | stage | core | marts

# -----------------------------
# JUPYTER CONFIGURATION
# -----------------------------
JUPYTER_TOKEN=dev